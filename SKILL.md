---
name: resume-rewriter-soda
description: Rewrite Chinese or English resumes and bullet points, tailor them to job descriptions using SODA capability mapping, and clarify transferable experience without inventing facts. Use for resume polishing, JD alignment, and cross-industry resume rewriting; not general career planning or cover letters.
---

# 简历重写 · SODA

将真实经历改写为清晰、突出贡献、便于招聘方理解的中英文简历。核心方法是「JD 对标 · 能力翻译」：用目标岗位的语言解释有证据的能力，保持原经历的事实与业务边界。

## 输入与任务范围

- 使用用户提供的简历或待改写段落、目标岗位、级别、地区、目标 JD，以及必须保留的信息。
- 仅询问无法从上下文判断、且会阻碍当前改写的最少信息。语言默认沿用原文；只有目标输出语言不清楚且影响交付时才询问。
- 没有 JD 时，直接做基础改写，不强制用户补齐 JD，也不将推测的岗位要求包装成真实 JD。
- 有 JD 时使用 SODA。用户只要求改一句、改一个模块或只要最终稿时，保持该范围，不强制输出整份分析。
- 默认交付可复制文本。多版本或文件格式按用户请求提供；改写任务本身不包含提交求职申请。

## 事实边界

- 不虚构或改变公司、职位、日期、学历、技能、项目、指标及职责归属；保留用户指定的不可变信息。
- 区分本人贡献与团队成果，不把「参与」升级为「主导」，不把接触过某工具写成精通，不把相似流程写成已具备目标行业资历。
- JD 是目标要求，不是候选人事实来源。每个新增能力表述都应能回溯到简历或用户补充的证据；无法确认时列为待澄清，不写成既定事实。
- 缺数字时，可建议补充处理量、耗时、错误率、覆盖人数或成本等指标类型。只有结果方向已确认时，才使用「错误率降低 [xx%]」这类占位；结果本身未确认时，不预设提升或下降，先描述真实交付。
- 指标保留原数值、单位、时间范围和统计口径，不任意换算「提升倍数」与百分比，不将相关性改成因果。
- 草稿中的占位符必须明显标记；用户需要直接投递的最终稿时，删除未确认断言与未填占位符，把补充建议放在正文之外。

## SODA 工作流

### S · Scan：拆解 JD

建立简洁的关键词地图，区分：

- **P0 硬门槛**：JD 明确要求的学历、年限、资格或必备技能。
- **P1 核心能力**：主要职责、关键流程、方法与领域知识。
- **P2 加分项**：JD 明确标为优先或加分的工具、证书和特定经验。

列出关键词、必要的同义词或常用缩写、优先级。不要将所有工具都归为加分项，按 JD 的实际措辞判断；含糊要求注明判断依据。真正缺失的硬门槛应明确指出，不能通过改写掩盖。

### O · Overlap：映射真实经历

用「JD 关键词 → 对应经历/证据 → 迁移解释」对照表寻找匹配。关注流程和机制是否相似，而非只匹配行业名称，例如规则配置、对账核算、异常预警、审计留痕、权限管理、多主体或多地区协作。

在表中区分直接匹配、可迁移经验和缺少证据。可迁移经验只说明方法上的联系，不等于做过目标岗位的业务。没有证据的要求不硬凑匹配。

### D · Decode：换表达，不换事实

按「动作 + 机制/方法 + 影响/结果」改写 bullet，借鉴 STAR/CAR，但不为凑结构补造背景或结果。缺少结果证据时，用真实交付或职责范围收尾。

自然融入有证据支持的 JD 术语，保留必要的原行业背景和项目名称，确保用户能解释每个表述。

例如：原文仅有「负责算薪平台」，不能直接改成「搭建统一结算与对账中台」。如果用户确认做过规则配置、金额核对和异常处理，可以写「在算薪平台中负责薪资规则配置、金额核对与异常处理」。没有做过的 AR/AP、VAT、SAP 等模块或工具，不因 JD 提到就加入简历。

### A · Amplify：调整结构与重点

在摘要、技能和每段经历内部优先突出最相关内容；保持公司与任职日期清楚，不打乱时间关系或暗示不存在的职业轨迹。

突出已有量化结果。必要时新增技能模块，但每项技能都应有真实依据；不为了关键词覆盖重复堆砌，也不承诺 ATS 通过率或虚构匹配分数。

## 文风与交付

- 使用明确动词和具体业务或技术名词，避免只有「赋能、推动、优化」而没有具体动作的表述。
- 英文以过去时描述过往岗位，当前持续职责用现在时，当前岗位中已完成的项目可用过去时。中文保持时间与完成状态清楚即可。
- 统一日期、标点、缩写和 bullet 风格。简历正文使用清晰标题与单层列表，避免依赖表格、多栏或图标表达关键信息。

完整 JD 对标任务默认输出：

1. **JD 关键词地图**：关键词、必要同义词与 P0/P1/P2。
2. **经历映射表**：关键词、证据、迁移解释及匹配状态。
3. **改写后的简历**：按适用模块提供摘要、技能、经历及其他原有必要内容，正文与分析分开，便于复制。
4. **缺失指标 / 需要澄清的问题**：仅列影响可信度或岗位匹配的关键项；没有问题则省略。

基础润色默认仅输出改写后的相关模块和必要澄清。用户指定的输出结构优先。

交付前核对：事实和指标是否一致；是否夸大职责或领域范围；每个关键词是否有依据；占位符是否清楚；是否完成用户要求的全部模块。
